# Proxy API requests to your backend
[[redirects]]
  from = "/api/*"
  to = "https://gconnect-dev.gdinexus.com:8409/api/:splat"
  status = 200
  force = true

# Fallback for React Router (SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
